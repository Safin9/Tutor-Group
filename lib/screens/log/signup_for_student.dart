import 'package:flutter/material.dart';

class SignUpForStudent extends StatelessWidget {
  const SignUpForStudent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Student Sign up'),
      ),
    );
  }
}
